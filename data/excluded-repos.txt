# excluded-repos.txt — repos that look Matrix-related but aren't.
#
# Format: one entry per line.
#   <host>/<owner>/<repo>    # reason
#
# Lines starting with `#` and blank lines are ignored. Comparison is
# case-insensitive against the canonical `<host>/<owner>/<repo>` form.
#
# All discovery and import scripts consult this file before creating
# a new project file. The build script also consults it as a defense in
# depth — even if a .md sneaks back in, the site will not show it.
#
# To exclude a project: append a line here with a clear reason and
# delete its `projects/<slug>.md` in the same commit.

github.com/jfsiii/XCSSMatrix    # CSS matrix transform polyfill, not Matrix protocol
codeberg.org/tybor/ml_linalg    # SIMD linear algebra library for Dart, not Matrix protocol
github.com/gyrdym/ml_linalg     # SIMD linear algebra library for Dart, not Matrix protocol
codeberg.org/sarah-quinones/faer    # Rust linear algebra library, not Matrix protocol
github.com/james-bowman/sparse    # Go sparse matrix library, not Matrix protocol
github.com/saehm/DruidJS    # JavaScript dimensionality reduction library, not Matrix protocol
codeberg.org/hzlb0l/Modula-2_Numeric_Library    # Modula-2 numerical math library, not Matrix protocol
github.com/jlmakes/rematrix    # CSS matrix() transform helper, not Matrix protocol
